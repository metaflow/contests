#include<bits/stdc++.h>
using namespace std;

using vi = vector<int>;
using ii = pair<int,int>;
using ll = long long;
using llu = unsigned long long;
const int INF = numeric_limits<int>::max();

int main() {
  int a, b, c, d;
  while (cin >> a >> b >> c >> d) {
    if (a <= b) {
      cout << "Second" << endl;
    } else {
      cout << "First" << endl;
    }
  }
}
